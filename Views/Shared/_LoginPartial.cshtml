@using Microsoft.AspNetCore.Identity
@using DAboneTakip.Areas.Identity.Data

@inject SignInManager<AuthDbUser> SignInManager
@inject UserManager<AuthDbUser> UserManager

    <link href="~/css/site.css" rel="stylesheet" />
<ul class="navbar-nav">
@if (SignInManager.IsSignedIn(User))
{
    <li class="nav-item">
        <a id="manage" class="w3-bar-item w3-button w3-hover-teal  my-button" style="text-decoration: none" asp-area="Identity" asp-page="/Account/Manage/Index" title="Manage">
        <i class="fas fa-user maric my-icon"></i>
        <label class="my-label"> Profil</label>@*Hello @UserManager.GetUserName(User)!*@
        </a>
    </li>
    <li class="nav-item">
        <form id="logoutForm" class="form-inline" asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Action("Index", "Home", new { area = "" })">
            <button id="logout" type="submit" class="w3-bar-item w3-button w3-hover-teal my-label  my-button" style="text-decoration: none;">
            <i class="fas fa-sign-out-alt maric my-icon" ></i>Çıkış Yap
            </button>
            
        </form>
    </li>
}
@*else
{
    <li class="nav-item">
        <a class="nav-link text-dark" id="register" asp-area="Identity" asp-page="/Account/Register">Register</a>
    </li>
    <li class="nav-item">
        <a class="nav-link text-dark" id="login" asp-area="Identity" asp-page="/Account/Login">Login</a>
    </li>
}*@
</ul>
